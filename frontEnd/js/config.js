// This is for static pages that are not served by the PHP backend.
// It assumes the static files are in the frontEnd/ directory.
window.BASE_URL = '../backEnd/public';
window.API_BASE_URL = '../backEnd/public/api';
